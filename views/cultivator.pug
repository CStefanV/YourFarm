doctype html 

mixin overviewBox(label,text)
    .overview-box__detail 
        span.overview-box__label= label
        span.overview-box__text= text 
mixin overviewBox2(text,label)
    .overview-box__detail 
        span.overview-box__text= text  
        span.overview-box__label= label  
html
    head 
        meta(charset='UTF-8') 
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href=' ../css/cultivator.css')
        title= cultivator.name
    body
        section.section-header
            .heading-box
                h1
                    span= cultivator.name
        section.section-description 
            .overview-box
                div 
                    .overview-box__group 
                        h2 Principalele informații despre acest cultivator                     
                        +overviewBox(`Lățime de lucru ${cultivator.workingWidth}`,'metri')
                        +overviewBox(`💨 ${cultivator.workingSpeed}`,'km/h')
                        +overviewBox( `🕒 ${cultivator.functioningHours}`,' ore funcționare')
                        +overviewBox(`🕒 ${cultivator.timeSpentInTheFarm}`,' luni petrecute în fermă')
                         
                    .overview-box__group 
                        h2 Alte informații despre cultivator
                        +overviewBox2('Închiriat/cumpărat: ',` ${cultivator.rented}`)
                        +overviewBox2(' Cai putere necesari ai tractorului: ',` ${cultivator.requiredTractorPower}`)

                         
            .description-box
                h2= `Descriere a cultivatorului ${cultivator.name}`
                .description__text= cultivator.description
            a(href="/listaCultivatoare")
                button.deleteBtnCultivator(data-cultivator-id=cultivator._id) Sterge cultivator
        script(src='/js/bundle.js')

                        