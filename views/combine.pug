doctype html 
include paginaPrincipala.pug

html
    head 
        meta(charset='UTF-8') 
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href='css/combine.css')
        title Combine 
    body
        main.main
            div
            h1 Dacă încă nu există combine sau doriți să mai adăugați vreuna în listă, apăsați butonul de mai jos
            a(href="/combine")
                button Adăugare combină


            .card-container
                each combina in combine  
                    .card
                        .card__header                                                      
                            h3
                                span= combina.name 
                        .card__details 
                            .card__data
                                span= `🐴 ${combina.power} cai putere`
                            .card__data
                                span= `🌽 ${combina.tank} litri` 
                            .card__data
                                span= `🕒 ${combina.functioningHours} ore funcționare` 
                            .card__data
                                span= `🕒 ${combina.timeSpentInTheFarm} luni petrecute în fermă`
                        .card__footer
                            a.btn.btn--green.btn--small(href=`/combina/${combina.slug}`) Detalii
        script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.4.0/axios.min.js')
