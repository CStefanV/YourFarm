doctype html 

mixin overviewBox(label,text)
    .overview-box__detail 
        span.overview-box__label= label
        span.overview-box__text= text 
mixin overviewBox2(text,label)
    .overview-box__detail 
        span.overview-box__text= text  
        span.overview-box__label= label  
html
    head 
        meta(charset='UTF-8') 
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href='../css/tractor.css')
        title= tractor.name
    body
        section.section-header
            .heading-box
                h1
                    span= tractor.name
        section.section-description 
            .overview-box
                div 
                    .overview-box__group 
                        h2 Principalele informații despre acest tractor                  
                        +overviewBox(`🐴 ${tractor.power}`,' cai putere')
                        +overviewBox(`💨 ${tractor.maxSpeed}`,' km/h')
                        +overviewBox( `🕒 ${tractor.functioningHours}`,' ore funcționare')
                        +overviewBox(`🕒 ${tractor.timeSpentInTheFarm}`,' luni petrecute în fermă')
                         
                    .overview-box__group 
                        h2 Alte informații despre tractor
                        +overviewBox2('Închiriat/cumpărat: ',` ${tractor.rented}`)
                        +overviewBox2('Categorie tractor: ',` ${tractor.category}`)
                        +overviewBox2('RPM maxim(rotații/minut): ',` ${tractor.maxRPM}`)
                         
            .description-box
                h2= `Descriere a tractorului ${tractor.name}`
                .description__text=`Folosit pentru: ${tractor.usedAt}.`
                .description__text=tractor.description  
            a(href="/listaTractoare")
                button.deleteBtn(data-tractor-id=tractor._id) Sterge tractor
            a(href=`/actualizareTractor/${tractor.slug}`)
                button.actualizareTractor Actualizeaza tractor

        //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.4.0/axios.min.js')
        script(src='/js/bundle.js')

        

                        