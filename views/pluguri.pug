doctype html 
include paginaPrincipala.pug

html
    head 
        meta(charset='UTF-8') 
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href='css/pluguri.css')
        title Pluguri
    body
        main.main
            h1 Dacă încă nu există pluguri sau doriți să mai adăugați vreunul în listă, apăsați butonul de mai jos
            a(href="/pluguri")
                button Adăugare plug


            .card-container

                each plug in pluguri  
                    .card
                        .card__header                                                   
                            h3
                                span= plug.name 
                        .card__details 
                            .card__data
                                span= `Lățime de lucru ${plug.workingWidth} metri`
                            .card__data
                                span= `💨 ${plug.workingSpeed} km/h` 
                            .card__data
                                span= `🕒 ${plug.functioningHours} ore funcționare` 
                            .card__data
                                span= `🕒 ${plug.timeSpentInTheFarm} luni petrecute în fermă`
                        .card__footer
                            a.btn.btn--green.btn--small(href=`/plug/${plug.slug}`) Detalii
        script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.4.0/axios.min.js')
