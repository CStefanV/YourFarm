doctype html 
include paginaPrincipala.pug

html
    head 
        meta(charset='UTF-8') 
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href='css/tractoare.css')
        title Tractoare 
    body
        main.main
            h1 Dacă încă nu există tractoare sau doriți să mai adăugați vreunul în listă, apăsați butonul de mai jos
            a(href="/tractoare")
                button Adăugare tractor

            .card-container
                each tractor in tractoare  
                    .card
                        .card__header                                                   
                            h3
                                span= tractor.name 
                        .card__details
                            h4= `Tractor ${tractor.category}` 
                            .card__data
                                span= `🐴 ${tractor.power} cai putere`
                            .card__data
                                span= `💨 ${tractor.maxSpeed} km/h` 
                            .card__data
                                span= `🕒 ${tractor.functioningHours} ore funcționare` 
                            .card__data
                                span= `🕒 ${tractor.timeSpentInTheFarm} luni petrecute în fermă`
                        .card__footer
                            a.btn.btn--green.btn--small(href=`/tractor/${tractor.slug}`) Detalii
        script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.4.0/axios.min.js')
