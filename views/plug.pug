doctype html 

mixin overviewBox(label,text)
    .overview-box__detail 
        span.overview-box__label= label
        span.overview-box__text= text 
mixin overviewBox2(text,label)
    .overview-box__detail 
        span.overview-box__text= text  
        span.overview-box__label= label  
html
    head 
        meta(charset='UTF-8') 
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href=' ../css/plug.css')
        title= plug.name
    body
        section.section-header
            .heading-box
                h1
                    span= plug.name
        section.section-description 
            .overview-box
                div 
                    .overview-box__group 
                        h2 Informații despre acest plug                       
                        +overviewBox(`Lățime de lucru ${plug.workingWidth}`,'metri')
                        +overviewBox(`💨 ${plug.workingSpeed}`,'km/h')
                        +overviewBox( `🕒 ${plug.functioningHours}`,' ore funcționare')
                        +overviewBox(`🕒 ${plug.timeSpentInTheFarm}`,' luni petrecute în fermă')
                         
                    .overview-box__group 
                        h2 Alte informații despre plug
                        +overviewBox2('Închiriat/cumpărat: ',` ${plug.rented}`)
                        +overviewBox2('Cai putere necesari ai tractorului: ',` ${plug.requiredTractorPower}`)
                         
            .description-box
                h2= `Descriere a plugului ${plug.name}`
                .description__text= plug.description
            a(href="/listaPluguri")
                button.deleteBtnPlug(data-plug-id=plug._id) Sterge plug
        script(src='/js/bundle.js')
        
                        